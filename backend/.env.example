# ============================================
# MINI-AMAZON BACKEND - ENVIRONMENT VARIABLES
# ============================================
#
# INSTRUCTIONS FOR DEPLOYMENT:
# 1. Copy this file and rename it to: .env
# 2. Replace the example values with your actual configuration
# 3. NEVER commit the .env file to Git (it's in .gitignore)
#
# IMPORTANT CONCEPT:
# - This .env file contains the ACTUAL VALUES used by the application
# - The config.py file only defines the STRUCTURE and TYPE VALIDATION
# - Pydantic automatically reads these values from .env at runtime
# - config.py should NOT contain real credentials, only this .env file
# ============================================

# ==================== DATABASE ====================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# In Docker: use service name 'db' as host
# Local dev: use 'localhost' as host
DATABASE_URL=postgresql://miniamazon:miniamazon123@db:5432/miniamazon

# ==================== SECURITY (JWT) ====================
# JWT Secret Key - MUST be at least 32 characters
# Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# CRITICAL: Change this in production!
SECRET_KEY=your-secret-key-change-this-in-production-min-32-chars

# JWT Algorithm (standard is HS256)
ALGORITHM=HS256

# Token expiration time in minutes
# Development: 30-1440 minutes
# Production: 15-60 minutes recommended
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ==================== CORS ====================
# Frontend URL for Cross-Origin Resource Sharing
# Development: http://localhost:5173
# Production: https://yourdomain.com
FRONTEND_URL=http://localhost:5173

# ==================== API SERVER ====================
# API server host (0.0.0.0 allows external connections)
API_HOST=0.0.0.0

# API server port
API_PORT=8000
